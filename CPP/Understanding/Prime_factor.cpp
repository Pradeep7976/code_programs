#include <iostream>
#include <string>

using namespace std;

void primefactor(int n)
{
    int max = -9;
    while (n % 2 == 0)
    {
        max = n / 2;
        n = n / 2;
    }
    for (int i = 3; i < n + 1; i += 2)
    {
        while (i != n % i = 0)
        {
            if (i % n = 0)
        }
    }
}
int main()
{
    return 0;
}