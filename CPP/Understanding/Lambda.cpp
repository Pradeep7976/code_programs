#include <bits/stdc++.h>
#define rep(a, b, c) for (int a = b; a < c; a++)
using namespace std;
int main()
{
    // int a = 1, b = 9;
    // auto yo = [&](int a, int b) //auto should be used so as it should not have any problem in conversion
    // { cout << a + b; };
    cout<<"kdf";
    return 0;
}